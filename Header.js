import React from 'react';
import Navbar from './Navbar';

const Header = () => {
  return (
    <header>
      {/* Include the navigation bar in the header */}
      <Navbar />
    </header>
  );
};

export default Header;